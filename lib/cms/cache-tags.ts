/** Every CMS read is cached under this tag so one save refreshes the whole site. */
export const CMS_CACHE_TAG = "msm-cms";

/** Media library reads (assets list) are tagged separately. */
export const MEDIA_CACHE_TAG = "msm-media";
