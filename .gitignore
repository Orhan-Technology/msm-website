# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code tooling
.claude/

# cms database + uploaded media (both retired by the move to Neon + Vercel Blob,
# still ignored so a leftover local copy never lands in a commit)
/data/
/public/uploads/

# local secrets pulled with `vercel env pull`
.env*
!.env.example

# one-off output of scripts/migrate-to-vercel.mjs
/cms-export.json
/blob-map.json
